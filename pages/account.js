import { Component } from 'react';
import { Wrapper, Products } from '../components';

class Account extends Component {
    render() {
        return (
            <Wrapper title = "Account Page">
                <div style={{ marginTop: `100px` }}>
                    <h2>Account page works</h2>
                </div>

            </Wrapper>
        );
    };
}

export default Account;
